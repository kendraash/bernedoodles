<div class=" box text-center">
  <h2>Upcoming Litters</h2>
  <hr>
  <% @litters.each do |litter| %>
    <h3><%=link_to "Due Date: #{litter.due_date.strftime('%B %d, %Y')}", litter_path(litter)%></h3>
    <% if litter.pictures.any? %>
      <%= litter.pictures[0].name(:medium) %>
    <% end %>
  <% end %>
</div>
